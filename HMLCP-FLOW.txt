═══════════════════════════════════════════════════════════════════════════════
                        HMLCP SYSTEM ARCHITECTURE FLOW
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│                         CLIENT (Browser)                                     │
│  👤 User Speaking → 🎤 Microphone → Audio Buffer                            │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │ Audio Data
                                   ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│                    CONFERENCE SERVER (Internal)                              │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────┐            │
│  │  STEP 1: Load User Profile & Generate Custom Vocabulary   │            │
│  │  ───────────────────────────────────────────────────────── │            │
│  │  • getUserProfile(username, language)                      │            │
│  │  • Load from: hmlcp/profiles/username_language.json        │            │
│  │  • Generate custom vocabulary from biasTerms               │            │
│  │    Example: ["Kubernetes:25", "Azure:25", ...]             │            │
│  └────────────────────────────────────────────────────────────┘            │
│                              ↓                                               │
└──────────────────────────────┬───────────────────────────────────────────────┘
                               │ Audio + Custom Vocabulary
                               ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│               🔴 EXTERNAL: DEEPGRAM STT API                                  │
│  ─────────────────────────────────────────────────────────────              │
│  Input:  Audio Buffer + Custom Keywords (Kubernetes:25, etc.)               │
│  Process: Speech Recognition with boosted technical terms                   │
│  Output: Raw Transcription + Confidence Score                               │
│                                                                              │
│  Example: "I need to check the thing in kubernetes"                         │
└──────────────────────────────┬───────────────────────────────────────────────┘
                               │ Raw Transcription
                               ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│                    CONFERENCE SERVER (Internal)                              │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────┐            │
│  │  STEP 2: Apply ULO Layer Processing (🟢 HMLCP Internal)   │            │
│  │  ───────────────────────────────────────────────────────── │            │
│  │  • uloLayer.apply(transcription)                           │            │
│  │  • Apply phrase mappings from user profile                 │            │
│  │  • Example mappings:                                       │            │
│  │    "check the thing" → "check the server status"           │            │
│  │    "the kubernetes" → "Kubernetes"                         │            │
│  │                                                             │            │
│  │  • profile.addTextSample(transcription)                    │            │
│  │    (Store for future pattern analysis)                     │            │
│  └────────────────────────────────────────────────────────────┘            │
│                              ↓                                               │
│         Processed Transcription: "I need to check the server status         │
│                                   in Kubernetes"                             │
└──────────────────────────────┬───────────────────────────────────────────────┘
                               │ Processed Text
                               ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│               🔴 EXTERNAL: DEEPL TRANSLATION API                             │
│  ─────────────────────────────────────────────────────────────              │
│  Input:  Processed Text (cleaned by ULO)                                    │
│  Process: Translate to target language                                      │
│  Output: Translated Text                                                    │
│                                                                              │
│  Example (en→es): "Necesito verificar el estado del servidor                │
│                    en Kubernetes"                                            │
└──────────────────────────────┬───────────────────────────────────────────────┘
                               │ Translated Text
                               ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│               🔴 EXTERNAL: AZURE TTS API                                     │
│  ─────────────────────────────────────────────────────────────              │
│  Input:  Translated Text                                                    │
│  Process: Text-to-Speech synthesis                                          │
│  Output: MP3 Audio Buffer                                                   │
└──────────────────────────────┬───────────────────────────────────────────────┘
                               │ Audio Data
                               ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│                         CLIENT (Browser)                                     │
│  🔊 Audio Output → Plays to User                                            │
└─────────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                            COMPONENT LEGEND
═══════════════════════════════════════════════════════════════════════════════

🔴 EXTERNAL SERVICES (3rd Party APIs):
   ├── Deepgram STT  - Speech-to-Text with custom vocabulary
   ├── DeepL         - Translation service
   └── Azure TTS     - Text-to-Speech synthesis

🟢 HMLCP INTERNAL (Your Custom Code):
   ├── User Profile Manager  - Stores linguistic characteristics
   ├── ULO Layer            - Real-time phrase mapping
   ├── Pattern Extractor    - Linguistic analysis
   └── Custom Vocab Gen     - Deepgram keyword generator

🔵 SERVER INTERNAL:
   ├── Socket.IO Handler    - WebSocket communication
   └── Translation Pipeline - Orchestrates all services

🟡 PERSISTENT STORAGE:
   └── hmlcp/profiles/*.json - User profile JSON files


═══════════════════════════════════════════════════════════════════════════════
                        KEY INTEGRATION POINTS
═══════════════════════════════════════════════════════════════════════════════

1. DEEPGRAM INTEGRATION (conference-server.js:106-110)
   ──────────────────────────────────────────────────────
   if (customVocab && customVocab.length > 0) {
     options.keywords = customVocab.map(v => `${v.phrase}:${v.boost}`);
     // Example: ["Kubernetes:25", "PostgreSQL:25", "Azure:25"]
   }

2. ULO PROCESSING (conference-server.js:366)
   ──────────────────────────────────────────
   const processedTranscription = uloLayer.apply(transcription);
   // Applies user-specific phrase mappings

3. DEEPL INTEGRATION (conference-server.js:400-404)
   ──────────────────────────────────────────────────
   const translatedText = await translateText(
     finalTranscription,  // ← ULO-processed text
     participant.language,
     targetParticipant.language
   );


═══════════════════════════════════════════════════════════════════════════════
                            DATA STRUCTURES
═══════════════════════════════════════════════════════════════════════════════

USER PROFILE (hmlcp/profiles/username_en.json):
┌─────────────────────────────────────────────────────┐
│ {                                                   │
│   "userId": "testuser",                             │
│   "language": "en",                                 │
│   "tone": "neutral",                                │
│   "phraseMap": {                                    │
│     "check the thing": "check the server status"    │
│   },                                                │
│   "biasTerms": [                                    │
│     "Kubernetes", "PostgreSQL", "Azure"             │
│   ],                                                │
│   "metrics": {                                      │
│     "intentMatchRate": 95.5,                        │
│     "calibrationIndex": 0.92                        │
│   }                                                 │
│ }                                                   │
└─────────────────────────────────────────────────────┘

CUSTOM VOCABULARY (Generated from biasTerms):
┌─────────────────────────────────────────────────────┐
│ [                                                   │
│   { "phrase": "Kubernetes", "boost": 25 },          │
│   { "phrase": "PostgreSQL", "boost": 25 },          │
│   { "phrase": "Azure", "boost": 25 }                │
│ ]                                                   │
│                                                     │
│ Sent to Deepgram as:                                │
│   keywords: ["Kubernetes:25", "PostgreSQL:25", ...] │
└─────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                        API ENDPOINTS (HMLCP)
═══════════════════════════════════════════════════════════════════════════════

GET  /api/hmlcp/profile/:userId/:language
     → Get user profile and statistics

POST /api/hmlcp/correction
     → Submit correction for learning
     Body: {
       "userId": "testuser",
       "language": "en",
       "rawInput": "check the thing",
       "correctedIntent": "check the server status"
     }

POST /api/hmlcp/analyze
     → Analyze linguistic patterns from collected samples
     Body: { "userId": "testuser", "language": "en" }

GET  /api/hmlcp/vocabulary/:userId/:language
     → Get custom vocabulary keywords for user

POST /api/hmlcp/save
     → Manually trigger profile save
     Body: { "userId": "testuser", "language": "en" }


═══════════════════════════════════════════════════════════════════════════════
                        AUTO-SAVE MECHANISM
═══════════════════════════════════════════════════════════════════════════════

Every 5 minutes, the server automatically saves all loaded profiles:

  setInterval(() => {
    for (const [key, { profile }] of userProfiles.entries()) {
      await profile.save();
    }
  }, 5 * 60 * 1000);

Profiles are saved to: hmlcp/profiles/<userId>_<language>.json


═══════════════════════════════════════════════════════════════════════════════
                        PERFORMANCE NOTES
═══════════════════════════════════════════════════════════════════════════════

• Profile Loading:        ~10ms (cached in memory after first load)
• ULO Processing:         <1ms (simple string replacements)
• Custom Vocab Gen:       <1ms (array mapping, done once per session)
• Pattern Analysis:       50-100ms (on-demand via API)
• Auto-save:              Async, no blocking (every 5 minutes)


═══════════════════════════════════════════════════════════════════════════════
